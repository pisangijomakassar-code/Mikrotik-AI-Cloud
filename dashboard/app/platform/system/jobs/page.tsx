import { PlatformPlaceholder } from "@/components/platform-placeholder"

export default function Page() {
  return (
    <PlatformPlaceholder
      title="Background Jobs"
      description="Cron, monitor, queue worker status"
    />
  )
}
