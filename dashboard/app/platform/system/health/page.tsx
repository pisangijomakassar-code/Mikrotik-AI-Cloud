import { PlatformPlaceholder } from "@/components/platform-placeholder"

export default function Page() {
  return (
    <PlatformPlaceholder
      title="Health Check"
      description="DB, Redis, agent connectivity status"
    />
  )
}
