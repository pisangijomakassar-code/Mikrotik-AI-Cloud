import { PlatformPlaceholder } from "@/components/platform-placeholder"

export default function Page() {
  return (
    <PlatformPlaceholder
      title="Plans & Pricing"
      description="Catalog plan + harga + fitur per tier"
    />
  )
}
