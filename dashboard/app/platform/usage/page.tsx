import { PlatformPlaceholder } from "@/components/platform-placeholder"

export default function Page() {
  return (
    <PlatformPlaceholder
      title="Per-Tenant Usage"
      description="Konsumsi resource per tenant (router, voucher, AI calls)"
    />
  )
}
