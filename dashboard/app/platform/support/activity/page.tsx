import { PlatformPlaceholder } from "@/components/platform-placeholder"

export default function Page() {
  return (
    <PlatformPlaceholder
      title="Tenant Activity"
      description="Read-only audit log per tenant — untuk debugging support"
    />
  )
}
